/**
 * Re-export of useTheme hook from ThemeContext
 * Provides access to theme state and setTheme function
 * Must be used within a ThemeProvider
 */
export { useTheme } from '../../contexts/ThemeContext';
